using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EmptyTile : GameBoardTile
{
    // Start is called before the first frame update
    void Start()
    {
        RegisterDragBehavior();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
